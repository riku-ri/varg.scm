on:
  workflow_dispatch:
  workflow_call:
jobs:
  test:
    runs-on: ubuntu-latest #public
    steps:
    - run: sudo apt install chicken-bin
    - run: chicken-install -s test
    - uses: actions/checkout@main
      with:
        submodules: recursive
    - run: chicken-install -n -l .
